import { ConsultantFilterPipe } from './consultant-filter.pipe';

describe('ConsultantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
