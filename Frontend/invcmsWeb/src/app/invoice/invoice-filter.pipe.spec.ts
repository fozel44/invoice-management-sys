import { InvoiceFilterPipe } from './invoice-filter.pipe';

describe('InvoiceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
