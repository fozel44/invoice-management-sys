import { RoleDeletePrefixPipe } from './role-delete-prefix.pipe';

describe('RoleDeletePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleDeletePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
